import { BodyScanIntroduction } from '@/modules/assessment';

/** A introdução do body scan, sem as abas: a medida do Student mora em Progresso (#316). */
export default function BodyScanRoute() {
  return <BodyScanIntroduction />;
}
