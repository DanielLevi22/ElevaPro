/**
 * O descanso quando a prescrição não diz: um minuto entre séries.
 *
 * Vale só para exercício sem `rest_seconds`. Zero prescrito continua zero — é o
 * especialista dizendo que não há descanso, e não uma lacuna a preencher.
 */
export const DEFAULT_REST_TIME = 60;
